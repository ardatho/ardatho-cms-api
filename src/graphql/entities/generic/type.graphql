type _Count {
  count: Int
}

input QueryInput {
  offset: Int
  limit: Int
  sort: String
  order: String
  fields: String
  search: String
  filter: String
  options: String
}

type Filter {
  value: ID!
  text: String
}

scalar ListInput
scalar Upload